spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/financial_dhentech_db
    username: administrator
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: false