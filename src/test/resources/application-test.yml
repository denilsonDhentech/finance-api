spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/financial_dhentech_db
    username: administrator
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: true